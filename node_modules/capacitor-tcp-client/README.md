# capacitor-tcp-client

send and receive data

## Install

```bash
npm install capacitor-tcp-client
npx cap sync
```

## API

<docgen-index>

* [`echo(...)`](#echo)
* [`connect(...)`](#connect)
* [`disconnect(...)`](#disconnect)
* [`send(...)`](#send)
* [`sendRaw(...)`](#sendraw)
* [`onData(...)`](#ondata)

</docgen-index>

<docgen-api>
<!--Update the source file JSDoc comments and rerun docgen to update the docs below-->

### echo(...)

```typescript
echo(options: { value: string; }) => Promise<{ value: string; }>
```

| Param         | Type                            |
| ------------- | ------------------------------- |
| **`options`** | <code>{ value: string; }</code> |

**Returns:** <code>Promise&lt;{ value: string; }&gt;</code>

--------------------


### connect(...)

```typescript
connect(options: { ipAddress: string; port: number; }) => Promise<{ success: boolean; client: number; }>
```

| Param         | Type                                              |
| ------------- | ------------------------------------------------- |
| **`options`** | <code>{ ipAddress: string; port: number; }</code> |

**Returns:** <code>Promise&lt;{ success: boolean; client: number; }&gt;</code>

--------------------


### disconnect(...)

```typescript
disconnect(options: { client: number; }) => Promise<{ success: boolean; client: number; }>
```

| Param         | Type                             |
| ------------- | -------------------------------- |
| **`options`** | <code>{ client: number; }</code> |

**Returns:** <code>Promise&lt;{ success: boolean; client: number; }&gt;</code>

--------------------


### send(...)

```typescript
send(options: { data: string; client: number; }) => Promise<{ result: any; }>
```

| Param         | Type                                           |
| ------------- | ---------------------------------------------- |
| **`options`** | <code>{ data: string; client: number; }</code> |

**Returns:** <code>Promise&lt;{ result: any; }&gt;</code>

--------------------


### sendRaw(...)

```typescript
sendRaw(options: { data: string; client: number; }) => Promise<{ result: any; }>
```

| Param         | Type                                           |
| ------------- | ---------------------------------------------- |
| **`options`** | <code>{ data: string; client: number; }</code> |

**Returns:** <code>Promise&lt;{ result: any; }&gt;</code>

--------------------


### onData(...)

```typescript
onData(options: { client: number; }) => Promise<{ result: any; }>
```

| Param         | Type                             |
| ------------- | -------------------------------- |
| **`options`** | <code>{ client: number; }</code> |

**Returns:** <code>Promise&lt;{ result: any; }&gt;</code>

--------------------

</docgen-api>
