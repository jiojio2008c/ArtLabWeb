import { WebPlugin } from '@capacitor/core';
import type { TCPClientPlugin } from './definitions';
export declare class TCPClientWeb extends WebPlugin implements TCPClientPlugin {
    NOT_SUPPORTED: string;
    constructor();
    echo(options: {
        value: string;
    }): Promise<{
        value: string;
    }>;
    connect(_options: {
        ipAddress: string;
        port: number;
    }): Promise<any>;
    disconnect(_options: {
        client: number;
    }): Promise<any>;
    send(_options: {
        data: string;
    }): Promise<any>;
    sendRaw(_options: {
        data: string;
    }): Promise<any>;
    onData(_options: {
        client: number;
    }): Promise<any>;
}
