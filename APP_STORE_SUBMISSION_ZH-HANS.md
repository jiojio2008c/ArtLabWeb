# MagicFloor App Store 上架文案（简体中文）

本文档中的“软件描述”和元数据可直接粘贴到 App Store Connect。审核账号、网址、法定版权主体和现场接收端信息需要在提交前由上架人员确认，文档不会虚构这些资料。

## 发布前替换项

- `[隐私政策网址]`：替换为已经创建并可公开访问的 HTTPS 隐私政策页面。
- `[技术支持网址]`：替换为已经创建并可公开访问的 HTTPS 技术支持页面。
- `[运营主体法定全称]`：替换为实际负责 MagicFloor 数据处理和用户支持的公司或个人法定名称。
- `[版权主体法定全称]`：替换为 App Store Connect 开发者账号对应的公司或个人法定名称。
- `[支持联系方式]`：替换为公开的支持邮箱或网站联系表单地址。

不要把方括号占位内容提交给 Apple。公开页面正文见 `APP_PRIVACY_POLICY_ZH-HANS.md` 和 `APP_TECHNICAL_SUPPORT_ZH-HANS.md`。

## App 信息

- App 名称：`MagicFloor`
- Bundle Identifier：`com.magicfloor.artlab`
- 支持设备：iPad
- 最低系统版本：iPadOS 15.0
- 当前版本：1.0（构建版本 1）

## 截图中的缺失项：直接填写内容

### 1. App 信息 → 内容版权

MagicFloor 会显示用户主动导入的图片、视频和音频，因此建议选择：

> 是，此 App 包含、显示或访问第三方内容，并且我拥有必要权利或已获得适用法律允许。

提交前应确认内置 Logo、背景、主题封面、遮罩、示例图片、视频、音效、字体及其他素材均为自有、已获授权或依法允许使用，尤其应核对仓库根目录中的示例媒体（例如 `466.mp3`、示例图片和视频）。建议保留原始创作文件、采购记录和授权书；本项目中的声明文案不能代替真实授权证明。

内部留档或审核补充说明可使用：

```text
MagicFloor 是一款创作与舞台控制工具。应用内随附的品牌、界面及示例素材由发布方拥有、已获授权或依法使用。用户只能导入其自行创作、拥有合法授权或依法可以使用的内容。应用不提供公开内容市场，也不会从第三方内容平台自动抓取或分发内容。
```

### 2. App 隐私 → 隐私政策网址

```text
[隐私政策网址]
```

将 `APP_PRIVACY_POLICY_ZH-HANS.md` 的正文发布到该网址。提交前用未登录浏览器和手机网络验证页面可以公开访问、无证书错误、无地区限制，且页面明确显示 MagicFloor 名称和联系方式。

### 3. 版本信息 → 版权

推荐格式：

```text
2026 [版权主体法定全称]
```

App Store Connect 通常不需要 `©`。版权主体必须是实际拥有本 App 权利的个人或公司；如果 `MagicFloor` 只是品牌名，不要用品牌名代替法定主体。

### 4. 构建版本 → 出口合规

当前代码未实现自研、专有或非标准加密算法，也没有 VPN、加密货币钱包或独立加密通信模块。账号登录只通过 iOS/WebKit 提供的标准 HTTPS/TLS 连接 Supabase，属于通常可豁免的标准加密用途。

对当前已经上传且显示“缺少出口合规证明信息”的构建版本，在 App Store Connect 的构建版本旁点击“管理”，按以下事实回答：

- 不使用专有或非标准加密算法。
- 不在 Apple 操作系统提供的加密之外另行实现加密算法。
- 仅使用系统提供的 HTTPS/TLS 保护账号连接。
- 不需要上传 CCATS 或其他出口合规附件。

供审核补充说明使用：

```text
MagicFloor does not implement proprietary or non-standard cryptographic algorithms. It only relies on standard HTTPS/TLS provided by iOS and WebKit for account authentication and service communication. The app does not contain non-exempt encryption functionality.
```

项目现已在 `Info.plist` 中加入 `ITSAppUsesNonExemptEncryption = NO`，新上传的构建会声明 App 不包含非豁免加密。若未来加入自研加密、VPN、端到端加密或其他独立密码功能，必须重新评估，不能继续沿用本结论。这个键不会改变已经上传的旧构建，因此截图中的当前构建仍需在 App Store Connect 手动完成一次出口合规回答。

### 5. App 隐私 → 数据处理问卷

当前构建应先选择“是，我们会从此 App 收集数据”，然后按下表填写。这里的“收集”是指发送到发布方或其服务提供商并可被其访问的数据；仅保存在 iPad 或只发送到用户自行配置的局域网设备，不等同于发布方收集。

| 数据类型 | 是否收集 | 用途 | 与用户身份关联 | 用于追踪 |
| --- | --- | --- | --- | --- |
| 联系信息 → 姓名 | 是 | App 功能 | 是 | 否 |
| 联系信息 → 电子邮件地址 | 是 | App 功能 | 是 | 否 |
| 标识符 → 用户 ID | 是 | App 功能 | 是 | 否 |

每一项都选择：

- 用途：只选择 `App 功能`。App Store Connect 没有独立的“账户管理”用途，登录和账号验证属于 App 功能。
- 与用户身份关联：`是`。
- 用于追踪：`否`。
- 不用于第三方广告、开发者广告或营销。

当前构建不应申报为发布方收集的数据：

- 用户导入或拍摄的图片、视频及录制音频：默认保存在当前 iPad；用户主动同步时只发送到其填写 IP 地址对应的局域网 MagicFloor 设备。
- 舞台作品、背景、物件、BGM、编排设置、局域网 IP 和端口：保存在当前设备，必要时发送到用户管理的配套设备。
- 精确位置、联系人、健康、财务、购买记录、浏览历史、搜索历史、使用情况、诊断、广告数据：当前代码未收集。

注意：当前账号资料接口还可能返回既有头像。若正式账号系统保存的是用户自行上传的真实头像，建议额外申报 `用户内容 → 照片或视频`，用途为 `App 功能`，与用户关联为 `是`，追踪为 `否`。若头像只是发布方提供的固定默认素材，则无需增加此项。

提交前还应在 Supabase 项目后台确认 Auth 审计日志是否长期保存并允许运营方访问 IP 地址、User-Agent 或其他安全日志。如果这些信息会被运营方长期访问或用于诊断，应按真实后台配置进一步评估 `诊断 → 其他诊断数据` 或其他适用数据类型；不能只根据前端代码推断为不收集。

### 6. 简体中文 → 关键词

以下内容低于 100 字符，可直接粘贴；使用英文半角逗号，不要加空格：

```text
互动艺术,舞台控制,动态艺术,艺术教育,展览控制,多媒体,现场演出,视觉创作,互动装置
```

### 7. 简体中文 → 技术支持网址

```text
[技术支持网址]
```

将 `APP_TECHNICAL_SUPPORT_ZH-HANS.md` 的正文发布到该网址。技术支持网址必须是公开网页，不能只填写邮箱、云盘文件或需要登录的后台页面。

## 副标题

动态艺术创作与互动舞台控制

## 宣传文本

在 iPad 上整理视觉素材、编排动态艺术作品，并连接 MagicFloor 配套展示系统，完成舞台预览、互动上传与现场控制。

## 软件描述

MagicFloor 是面向艺术教育、展览与互动空间的 iPad 创作与控制工具。你可以在 iPad 上整理视觉素材、编排动态艺术作品，并将内容发送到同一局域网内的 MagicFloor 舞台播放电脑或互动装置进行展示。

【动态艺术创作】

建立作品档案与文件夹，集中管理背景、物件和音频素材。使用图片或视频制作舞台背景，并设置固定、顺序或随机播放、切换间隔、背景音乐与转场效果。

在可视化舞台中安排物件的位置和前后层级，调整大小、旋转与翻转，设置出场时间、出场顺序、移动方式、目标位置、动画和音效。物件可以应用于全部背景，也可以只在指定背景中出现。

【舞台预览与控制】

直接在控制页预览背景切换、物件移动、出场动画、转场和音频效果，也可以单独播放当前选中的背景。作品准备完成后，可将背景、物件、音频及舞台参数同步到已配置的 MagicFloor 接收端，配合现场展示进行播放与操作。

应用同时提供远程键盘控制界面，可向配套设备发送快捷操作、方向和音量等控制指令。

【互动艺术】

选择互动主题，从照片图库导入图片或使用相机拍摄，搭配内置遮罩调整画面位置和大小，再将处理后的内容发送到同一局域网中的 MagicFloor 互动装置，让图像进入对应的互动场景。

【使用环境与数据说明】

使用本应用需要已开通的 MagicFloor 账号。账号登录与资料读取需要访问在线账号服务；舞台播放、作品同步、互动上传和远程控制等功能，需要 iPad 与兼容的 MagicFloor 播放电脑或互动装置连接到同一局域网，并在设置中填写对应的 IP 地址与端口。

动态艺术作品档案、素材和编辑设置主要保存在当前 iPad。本应用目前不提供作品云端同步或公开社区发布功能。只有在用户主动进行预览、同步、互动上传或控制时，相关素材、舞台参数和控制指令才会发送到用户配置的 MagicFloor 配套设备。相机、照片图库和麦克风只会在用户主动选择拍摄、导入或录制内容时使用。

## App Review 审核备注模板

以下内容应填写在 App Store Connect 的“审核备注”，不要直接放入公开的软件描述。

```text
MagicFloor 是面向配套展示环境的 iPad 创作与舞台控制应用。

1. 登录
本 App 启动后需要登录。账号由 MagicFloor 服务方预先建立,并分发，App 内不提供自行注册入口。
审核账号：[mfc.pc03@gmail.com]
审核密码：[123456]

2. 建议审核路径
- 登录后选择“动态艺术”。
- 打开现有测试作品，或建立作品并添加背景与物件。
- 进入控制页后可直接使用 iPad 本地预览，检查背景切换、物件动画和音频设置。
- “互动艺术”可使用照片图库或相机选择画面，并进入遮罩调整流程。

3. 配套环境
跨设备舞台同步、互动上传和远程控制需要 iPad 与 MagicFloor 接收端位于同一局域网，并在设置中填写接收端 IP 与端口。
审核期间可用的接收端信息：[提交前填写；如不提供，请明确说明审核人员可仅验证本地预览流程]

4. 权限用途
- 相机：仅在用户主动选择拍摄作品时使用。
- 照片图库：仅在用户主动导入所选作品时使用。
- 麦克风：仅在用户主动录制音频时使用。
- 本地网络：用于连接用户配置的 MagicFloor 播放电脑或互动装置。
```

## 提交前必须补齐

- 可用的审核账号与密码。
- 把隐私政策和技术支持正文发布到已创建的网站，并替换文档内所有方括号占位内容。
- 在“App 信息”完成内容版权选择。
- 在简体中文版本填写版权、关键词、隐私政策网址和技术支持网址。
- 由具有“管理员”或“App 管理”权限的用户发布 App 隐私问卷答案。
- 对当前已上传构建完成出口合规回答；后续构建已通过 `ITSAppUsesNonExemptEncryption = NO` 声明不包含非豁免加密。
- 新增的 `PrivacyInfo.xcprivacy` 已声明账号姓名、电子邮件地址、用户 ID、无追踪以及 Capacitor Filesystem 所需的文件时间戳 API 使用理由；在 Xcode Archive 后确认该文件被包含在 App 包内。
- 如审核需要验证跨设备功能，提供可访问的接收端环境、操作视频或与审核团队约定的测试方式。
